/* eslint-disable */
/**
 * 이 파일은 `pnpm admin:generate-openapi-types` 명령으로 생성됩니다.
 * 최신 스펙 반영을 위해 로컬에서 해당 스크립트를 실행하고 커밋해 주세요.
 */

export interface paths {}
export interface components {}
export interface operations {}
export interface external {}
